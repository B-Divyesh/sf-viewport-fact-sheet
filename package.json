{
  "name": "viewport-fact-sheet",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "concurrently \"wxt\" \"vite --config site/vite.config.ts\" \"esbuild src/injected.ts --bundle --format=iife --target=chrome110 --outfile=.output/chrome-mv3/picker.js --watch\"",
    "build:extension": "wxt build && esbuild src/injected.ts --bundle --format=iife --target=chrome110 --minify --outfile=.output/chrome-mv3/picker.js && node scripts/package-extension.mjs",
    "build:helper": "node scripts/build-helper.mjs && node scripts/write-helper-types.mjs",
    "build:site:pages": "vite build --config site/vite.config.ts",
    "build:site": "node scripts/clean.mjs && npm run build:extension && npm run build:helper && npm run build:site:pages && node scripts/package-release.mjs && node scripts/verify-release.mjs",
    "build": "npm run build:site",
    "test": "vitest run && npm run build && npm run test:e2e",
    "test:unit": "vitest run",
    "test:e2e": "xvfb-run -a playwright test"
  },
  "devDependencies": {
    "@axe-core/playwright": "4.10.2",
    "@playwright/test": "^1.58.2",
    "@types/chrome": "^0.0.330",
    "concurrently": "^9.2.1",
    "esbuild": "^0.25.11",
    "happy-dom": "^20.11.12",
    "playwright": "1.58.2",
    "playwright-core": "^1.58.2",
    "typescript": "^5.9.3",
    "vite": "^6.4.3",
    "vitest": "^3.2.7",
    "wxt": "^0.21.4"
  }
}
