# Viewport Fact Sheet

Live: https://viewport-fact-sheet.sociobot.in — built by the Param Factory (`browser-extension`).

See `.factory/brief.json` for the researched problem this solves and `.factory/design.md` for the visual system.

## Develop

```
npm install
npm run dev
npm test
npm run build   # -> dist/
```
